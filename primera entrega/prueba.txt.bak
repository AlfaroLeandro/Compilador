DIM < a, b, z, var2, h> AS < integer, real, string, string, Int >;
DIM < pivot, contador, promedio, actual, suma> AS < integer, integer, real, real, real >;
DISPLAY "Prueba.txt LyC Tema 2!";
DISPLAY "Ingrese un valor entero para actual: ";

pivot := 0b11110;
GET actual;
contador := 0;
suma := 02.5+pivot;
while (contador <= 92) 
	contador := contador + 1;
	actual := (contador/0.342) + (contador* 0xFF);
	suma := suma + actual;
endwhile;

DISPLAY "La suma es: ";
DISPLAY suma;

if (actual > 2 && !(actual == 0)){
	DISPLAY "actual es mayor que 2 y distinto de cero";
else
	if (actual < 2)
	DISPLAY “no es mayor que 2”
	endif;
endif;